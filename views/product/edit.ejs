
<!DOCTYPE html>
<html>
    <head>
        <title>My Website</title>
        <link rel='stylesheet' href='/bootstrap/dist/css/bootstrap.min.css' />
        <link rel='stylesheet' href='/@fortawesome/fontawesome-free/css/all.min.css' />
        <link rel='stylesheet' href='/stylesheets/style.css' />
        <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.2.1/dist/js/bootstrap.bundle.min.js"></script>
    </head>
  <body>
    <main class="container">
        <div class="row">
            <h1 id="textLarge"><%= title %></h1>
        <br>
            <div>
                <a href="/business/add" class="btn btn-primary align-self-end" role="button">
                    <i class="fas fa-plus-circle"></i>
                    Add New Contact
                </a>
            </div>
        <br/>
            <div class="table-responsive">
                <table class="table table-bordered table-striped table-hover">
                    <!-- Header Row-->
                    <tr>
                        <th class="text-center">Name</th>
                        <th class="text-center">Brand</th>
                        <th class="text-center">Category</th>
                        <th class="text-center">Quantity</th>                      //aalmario
                        <th class="text-center">Price</th>                         //aalmario
                        <th class="text-center">Description</th>                   //aalmario
                        <th class="text-center" colspan="2">Actions</th>
                    </tr>
                    <% for(let count=0; count < product.length; count++) { %>
                        <tr>
                            <td class="text-center"><%= product[count].name %></td>
                            <td class="text-center"><%= product[count].brand %></td>
                            <td class="text-center"><%= product[count].category %></td>
                            <td class="text-center"><%= product[count].quantity %></td>        //aalmario
                            <td class="text-center"><%= product[count].price %></td>           //aalmario
                            <td class="text-center"><%= product[count].description %></td>     //aalmario
                            <td class="text-center">
                                <a href="/product/edit/<%= product[count]._id %>" class="btn btn-primary btn-sm">
                                <i class="fas fa-pencil-alt"></i></a>
                            </td>
                            <td class="text-center">
                                <a href="/business/delete/<%= product[count]._id %>" class="btn btn-danger btn-sm">
                                <i class="fas fa-trash-alt"></i></a>
                            </td>
                        </tr>
                        <% } %>
                    </tr>
                    <% } %>
                </table>
            </div>
        </div>
    </main>
</body>
</html>


